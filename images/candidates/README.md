# Candidate Images

Put unused Spanish Lab source images here before running the episode planner.

The planner may inspect this directory and propose episode briefs, but it must not move or delete candidate images. Once the showrunner greenlights an episode, the producer copies the selected image into the normal `images/{slug}.png` path.
